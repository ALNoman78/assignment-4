function reverseArray(Array) {
    let rev = [];
    for (let i = 0; i < Array.length; i++) {
        if (Array[i] ===) {
            
        }
    }
    // return
}
const arr = [12,45,65,789,9,45,67]
console.log(reverseArray(arr));